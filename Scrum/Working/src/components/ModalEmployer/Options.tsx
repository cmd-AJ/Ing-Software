import { IonButton } from "@ionic/react"
import React from "react"

interface ContainerProps {  }

const Options: React.FC<ContainerProps> = () => {
    return (
        <div>
            <IonButton></IonButton>
        </div>
    )
}