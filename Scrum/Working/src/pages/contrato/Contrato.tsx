import React from "react";
import styles from "./contrato.module.css"; // CSS module for page-specific styles

const Contrato: React.FC = () => {
  return <div className={styles.contratoPage}></div>;
};

export default Contrato;
